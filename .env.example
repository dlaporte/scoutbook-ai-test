MCP_BASE_URL=https://mcp.example.com
LOG_LEVEL=INFO
AUTH_CODE_TTL=300
TRANSACTION_TTL=900
# STORAGE_ENCRYPTION_KEY=  # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
